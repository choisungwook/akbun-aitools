# Diagram Inputs Are Normalized to Topology

Kubernetes network diagram skill은 자연어, 사진, YAML, 기존 draw.io 파일을 별도 모드로 분리하지 않고 topology로 정규화한다. 입력이 불완전해도 보수적 가정과 `확인 필요`를 분리해 다이어그램 생성을 진행하는 쪽이, 초기 입력 형식을 엄격히 요구하는 것보다 작업 흐름을 덜 막는다.
